#!/usr/bin/env perl
use strict;
use lib "lib";
use Plack::Runner;

my $runner = Plack::Runner->new(server => 'Twiggy', env => 'production');
$runner->parse_options(@ARGV);
$runner->run;

__END__

=head1 NAME

twiggy - Twiggy launcher

=head1 SYNOPSIS

  twiggy --listen :9090

Run C<plackup -h> for more options.

=head1 SEE ALSO

L<AnyEvent::Server::PSGI::Twiggy>

=cut
